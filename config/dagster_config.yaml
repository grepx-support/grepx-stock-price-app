# Dagster configuration
app:
  name: "price_app"
  version: "1.0.0"

dagster:
  module_name: "price_app"
  home: ${oc.env:PROJECT_ROOT}/../dagster_home

# Assets configuration - Use infrastructure/workflows/dagster_workflow.py instead
# These sections are required by Dagster framework (even if empty)
assets:
  modules: []
  directories: []

ops:
  modules: []
  directories: []
